# Countdown - Prelaunch page — Viewpoints

No packaged reference screen matches a standalone countdown. Viewpoints limited to the
explicit source:

- Verify display of title and the three countdown units (labels + 2-digit boxes).
- Verify units auto-update over time and stay 2-digit / within their value ranges.
- Verify Days shows "00" when less than 1 day remains.

## Discarded (not in design — do NOT generate)
- Any interaction, navigation, redirect-at-zero, or input behavior (none specified here).
