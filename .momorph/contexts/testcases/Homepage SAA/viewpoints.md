# Homepage SAA — Viewpoints (filtered to explicit source)

Reference packaged files that overlap: Global_navigation, Fixed_Navigation, Notification,
Links. Kept only viewpoints that refine behavior already present in this design; none add
new behavior.

## Global / Fixed navigation (header)
- Verify sticky header stays fixed at top while scrolling.
- Verify active nav item styling (selected vs hover vs normal).
- Verify each nav link navigates to its target page.

## Notification
- Verify unread badge appears only when there are unread notifications.
- Verify clicking the bell opens the notification panel.

## Links (cards / CTAs / footer)
- Verify each link/button navigates to the correct target.
- Verify award card link includes the category hash anchor and the target page scrolls to it.

## Discarded (not in design — do NOT generate)
- Search, pagination, sorting, login forms, ranking tables, notification mark-as-read details
  (panel internals belong to the Notification panel screen, not this page).
